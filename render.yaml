services:
  - type: web
    name: esticure-backend
    env: node
    buildCommand: cd backend && npm install && npm run build
    startCommand: node backend/dist/main
    envVars:
      - key: NODE_ENV
        value: production
